<h1>タスク管理</h1>
<% if logged_in? %>
     <h2><%= current_user.name =%>のタスク一覧</h2>
     

    <ul>
        <% @tasks.each do |task| %>
            <li><%= link_to task.id, task %> ：　<%= task.content %> /ステータス[<%= task.status %>]</li>
        <% end %>
    </ul>
    
    <%= link_to "新規追加", new_task_path %>
<% else %>

    <p>ログインもしくは新規ユーザ登録をしてください。</p>
    <p></p><%= link_to "ログイン", login_path %></p>
    <p><%= link_to "新規登録", signup_path %></p>
<% end %>